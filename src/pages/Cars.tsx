import Layouts from "../layouts";
import TableComponent from "../components/Table";

const Cars: React.FC = () => {
  return (
    <Layouts>
      <TableComponent />
    </Layouts>
  );
};

export default Cars;
